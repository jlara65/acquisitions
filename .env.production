# Production env for Docker Compose and deployment
NODE_ENV=production
PORT=3000
LOG_LEVEL=info
# Use the actual Neon Cloud Database URL (do not commit real secrets)
# Example: postgresql://<user>:<password>@<endpoint>.neon.tech/<db>?sslmode=require
DATABASE_URL=postgresql://<user>:<password>@<endpoint>.neon.tech/<db>?sslmode=require

# Other app envs
# ARCJET_KEY=ajkey_prod_xxx
